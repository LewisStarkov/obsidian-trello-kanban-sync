{
	"name": "trello-kanban-sync",
	"version": "0.1.2",
	"description": "Mirrors Trello boards into native Kanban-plugin boards on an interval, with optional two-way sync.",
	"main": "main.js",
	"scripts": {
		"dev": "node esbuild.config.mjs",
		"build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production"
	},
	"license": "MIT",
	"devDependencies": {
		"@types/node": "^20.0.0",
		"esbuild": "^0.20.0",
		"obsidian": "^1.4.11",
		"typescript": "^5.4.0"
	}
}
